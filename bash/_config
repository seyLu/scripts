#!/bin/bash

#------------------------------------------------------------------------#
# Configurations
#------------------------------------------------------------------------#

[[ -z "$_DEBUG" ]] && _DEBUG="0"
export _DEBUG

declare _PYTHON="python3.12"
export _PYTHON

[[ -z "$_EDITOR" ]] && _EDITOR="zed"
export _EDITOR

[[ -z "$_RESUME_FILE" ]] && _RESUME_FILE="mj_sabit_resume.pdf"
export _RESUME_FILE

[[ -z "$_WORK" ]] && _WORK="digitizing.io"
export _WORK
